diff --git a/src/google/protobuf/compiler/command_line_interface.cc b/src/google/protobuf/compiler/command_line_interface.cc
--- a/src/google/protobuf/compiler/command_line_interface.cc	(revision 909a0f36a10075c4b4bc70fdee2c7e32dd612a72)
+++ b/src/google/protobuf/compiler/command_line_interface.cc	(date 1758192793953)
@@ -262,11 +262,15 @@
     return;
   }
   // Check if the upper level directory has an "include" subdirectory.
-  pos = path.find_last_of("/\\");
-  if (pos == std::string::npos || pos == 0) {
-    return;
-  }
-  path = path.substr(0, pos);
+  // change "'$/bin' is next to 'include'" assumption to "'$/bin/tools' is next to 'include'"
+  for (int i = 0; i < 2; i++)
+  {
+      pos = path.find_last_of("/\\");
+      if (pos == std::string::npos || pos == 0) {
+        return;
+      }
+      path = path.substr(0, pos);
+  }
   if (IsInstalledProtoPath(path + "/include")) {
     paths->push_back(
         std::pair<std::string, std::string>("", path + "/include"));
